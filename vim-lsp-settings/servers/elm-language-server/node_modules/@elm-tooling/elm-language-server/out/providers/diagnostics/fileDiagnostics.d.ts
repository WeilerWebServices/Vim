import { Diagnostic } from "vscode-languageserver";
export declare const enum DiagnosticKind {
    ElmMake = 0,
    ElmAnalyse = 1,
    ElmTest = 2,
    TypeInference = 3
}
export declare function diagnosticsEquals(a: Diagnostic, b: Diagnostic): boolean;
export declare class FileDiagnostics {
    uri: string;
    private diagnostics;
    constructor(uri: string);
    get(): Diagnostic[];
    update(kind: DiagnosticKind, diagnostics: Diagnostic[]): boolean;
    getForKind(kind: DiagnosticKind): Diagnostic[];
}
