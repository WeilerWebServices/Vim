import { Hover, TextDocumentPositionParams } from "vscode-languageserver";
import { IElmWorkspace } from "../elmWorkspace";
declare type HoverResult = Hover | null | undefined;
export declare class HoverProvider {
    private connection;
    constructor();
    protected handleHoverRequest: (params: TextDocumentPositionParams, elmWorkspace: IElmWorkspace) => HoverResult;
    private createMarkdownHoverFromDefinition;
}
export {};
