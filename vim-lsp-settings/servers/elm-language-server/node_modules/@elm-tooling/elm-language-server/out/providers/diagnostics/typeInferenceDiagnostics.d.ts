import { CodeAction, CodeActionParams, Diagnostic } from "vscode-languageserver";
import { IElmWorkspace } from "../../elmWorkspace";
import { Tree } from "web-tree-sitter";
export declare class TypeInferenceDiagnostics {
    TYPE_INFERENCE: string;
    private elmWorkspaceMatcher;
    constructor();
    createDiagnostics: (tree: Tree, uri: string, elmWorkspace: IElmWorkspace) => Diagnostic[];
    onCodeAction(params: CodeActionParams): CodeAction[];
    private filterTypeInferenceDiagnostics;
    private convertDiagnosticsToCodeActions;
    private insertQuickFixAtStart;
    private getNodeRange;
}
