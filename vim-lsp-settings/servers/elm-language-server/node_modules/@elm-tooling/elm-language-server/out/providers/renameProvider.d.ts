import { PrepareRenameParams, Range, RenameParams, TextDocumentEdit, TextEdit, WorkspaceEdit } from "vscode-languageserver";
import { SyntaxNode } from "web-tree-sitter";
import { IElmWorkspace } from "../elmWorkspace";
export declare class RenameProvider {
    private connection;
    constructor();
    protected handleRenameRequest: (params: RenameParams, elmWorkspace: IElmWorkspace) => WorkspaceEdit | null | undefined;
    protected handlePrepareRenameRequest: (params: PrepareRenameParams, elmWorkspace: IElmWorkspace) => Range | null;
    static getRenameEdits(affectedNodes: {
        originalNode: SyntaxNode;
        references: {
            node: SyntaxNode;
            uri: string;
        }[];
    } | undefined, newName: string): [{
        [uri: string]: TextEdit[];
    }, TextDocumentEdit[]];
    private createModuleDeclarationRenameChange;
    uppercaseNewNameIfModuleDeclaration(newName: string, affectedNodes: {
        originalNode: SyntaxNode;
        references: {
            node: SyntaxNode;
            uri: string;
        }[];
    } | undefined): string;
    private generateUriFromModuleName;
}
