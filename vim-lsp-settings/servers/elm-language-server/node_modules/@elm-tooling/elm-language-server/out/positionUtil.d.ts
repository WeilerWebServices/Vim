import { Position as VSPosition } from "vscode-languageserver";
import { Point as TSPosition } from "web-tree-sitter";
export declare class PositionUtil {
    static FROM_VS_POSITION(position: VSPosition): PositionUtil;
    static FROM_TS_POSITION(position: TSPosition): PositionUtil;
    private row;
    private col;
    constructor(row: number, col: number);
    toVSPosition(): VSPosition;
    toTSPosition(): TSPosition;
}
export declare function comparePosition(pos1: VSPosition, pos2: TSPosition): number;
