import { IElmWorkspace } from "src/elmWorkspace";
import { SyntaxNode } from "web-tree-sitter";
import { IReferenceNode } from "./referenceNode";
export declare class References {
    static find(definitionNode: IReferenceNode | undefined, elmWorkspace: IElmWorkspace): {
        node: SyntaxNode;
        uri: string;
    }[];
    static findOperator(node: SyntaxNode): SyntaxNode | undefined;
    private static findFunctionCalls;
    private static findAllFunctionCallsAndParameters;
    private static findParameterUsage;
    private static findAllRecordBaseIdentifiers;
    private static getFunctionAnnotationNameNodeFromDefinition;
    private static findFieldUsages;
    private static getFieldReferences;
}
