import { IElmWorkspace } from "src/elmWorkspace";
import { Position } from "vscode-languageserver";
import { SyntaxNode } from "web-tree-sitter";
export declare class RenameUtils {
    static getRenameAffectedNodes(elmWorkspace: IElmWorkspace, uri: string, position: Position): {
        originalNode: SyntaxNode;
        references: {
            node: SyntaxNode;
            uri: string;
        }[];
    } | undefined;
}
