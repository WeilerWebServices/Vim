import { DocumentFormattingParams, TextEdit } from "vscode-languageserver";
import { URI } from "vscode-uri";
import { IElmWorkspace } from "../elmWorkspace";
declare type DocumentFormattingResult = Promise<TextEdit[] | undefined>;
export declare class DocumentFormattingProvider {
    private events;
    private connection;
    private settings;
    constructor();
    formatText: (elmWorkspaceRootPath: URI, elmFormatPath: string, text: string) => DocumentFormattingResult;
    protected handleFormattingRequest: (params: DocumentFormattingParams, elmWorkspace: IElmWorkspace) => DocumentFormattingResult;
}
export {};
