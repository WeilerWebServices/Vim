import { DidChangeTextDocumentParams, DidOpenTextDocumentParams, Event } from "vscode-languageserver";
import { Tree } from "web-tree-sitter";
import { IElmWorkspace } from "../elmWorkspace";
export declare class ASTProvider {
    private connection;
    private parser;
    private documentEvents;
    private treeChangeEvent;
    readonly onTreeChange: Event<{
        uri: string;
        tree: Tree;
    }>;
    constructor();
    protected handleChangeTextDocument: (params: DidChangeTextDocumentParams | DidOpenTextDocumentParams, elmWorkspace: IElmWorkspace) => void;
    private getEditFromChange;
    private textToPosition;
    private getIndexesFromRange;
    private addPositions;
    private toTSPoint;
}
