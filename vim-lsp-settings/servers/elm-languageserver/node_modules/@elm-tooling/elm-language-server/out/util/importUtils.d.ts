import { IForest, ITreeContainer } from "../forest";
import { NodeType } from "./treeUtils";
import { SyntaxNode } from "web-tree-sitter";
interface IPossibleImport {
    module: string;
    value: string;
    type: NodeType;
    node: SyntaxNode;
    valueToImport?: string;
    package?: string;
}
export declare class ImportUtils {
    static getPossibleImportsFiltered(forest: IForest, uri: string, filterText: string): IPossibleImport[];
    static getPossibleImports(forest: IForest, uri: string): IPossibleImport[];
    static getPossibleImportsOfTree(tree: ITreeContainer): IPossibleImport[];
    private static comparisonScore;
}
export {};
