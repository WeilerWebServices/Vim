import { SyntaxNode } from "web-tree-sitter";
import { EFieldType } from "./expressionTree";
export declare class RecordFieldReferenceTable {
    private refsByField;
    private _frozen;
    get frozen(): boolean;
    constructor(refsByField?: Map<string, SyntaxNode[]>);
    static fromExpressions(fieldExpressions: EFieldType[]): RecordFieldReferenceTable;
    get(field: string): SyntaxNode[];
    addAll(other: RecordFieldReferenceTable): void;
    plus(other: RecordFieldReferenceTable): RecordFieldReferenceTable;
    isEmpty(): boolean;
    freeze(): void;
}
